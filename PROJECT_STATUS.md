# وضعیت پروژه — شهرام روغن

آخرین بروزرسانی: 2026-08-24

## معرفی

فروشگاه آنلاین روغن موتور و فرآورده‌های خودرو. کلاینت با React + Vite + Tailwind، سرور با Express + MongoDB (Atlas). دو زبانه (فارسی/انگلیسی)، دارک‌مود، پنل مدیریت کامل، حساب کاربری مشتری.

## ساختار کلی

```
client/   React + Vite (Tailwind v4)
  src/components/   کامپوننت‌های صفحه‌محور (هرکدام index.js + sections/hooks داخلی)
  src/pages/        صفحات نازک که فقط کامپوننت مربوطه را رندر می‌کنند (مسیریابی از اینجا)
  src/context/       Cart, Order, Product, CustomerAuth, Theme, Language
  src/services/      لایه‌ی axios برای صدا زدن API
  src/data/          داده‌های ثابت (برندها، دسته‌بندی‌ها، منو، ترجمه‌ها)

server/   Express + Mongoose
  controllers/ routes/ services/ repositories/ models/ middleware/ validations/
  uploads/products/   عکس‌های آپلودی محصولات (gitignore شده)
  templates/          فایل‌های نمونه‌ی اکسل برای ایمپورت/بروزرسانی گروهی
```

## وضعیت فعلی

- مهاجرت کامل از ذخیره‌سازی JSON به MongoDB انجام شده.
- پنل ادمین: مدیریت محصولات (تک‌تک + ایمپورت گروهی اکسل + بروزرسانی گروهی قیمت)، مدیریت سفارش‌ها، داشبورد آماری.
- حساب کاربری مشتری (ثبت‌نام/ورود جدا از ادمین).
- ۲۹۸ محصول فعال در دیتابیس، همگی به‌درستی به یکی از ۱۷ برند تعریف‌شده در `client/src/data/brands.js` متصل‌اند.

## کارهای انجام‌شده در این سشن

**رفع باگ‌ها:**
- باگ اصلی: فیلتر صفحه‌ی برند (`/brand/:brand`) روی نام فارسی برند مقایسه می‌کرد ولی بخشی از محصولات (ایمپورت‌شده از اکسل) با نام انگلیسی/نامنطبق ذخیره شده بودند → هیچ محصولی نمایش داده نمی‌شد. ۲۳۰ رکورد در دیتابیس نرمال‌سازی شد و ایمپورتر اکسل حالا برند را در برابر لیست رسمی برندها (`server/data/brands.js`) اعتبارسنجی می‌کند.
- متن انگلیسی زیر لوگوی هدر به‌خاطر یک قانون CSS سراسری (`p { color: var(--text-main) }` خارج از لایه‌ی Tailwind) هم‌رنگ پس‌زمینه می‌شد و دیده نمی‌شد → override اختصاصی هدر اضافه شد.
- صفحه‌ی `/admin/orders` فاقد `AdminHeader` بود (نمی‌شد به بقیه‌ی سایت لینک برگشت زد) → اضافه شد.

**UI:**
- فاویکون با لوگوی برند (زرد، بولدتر) هماهنگ شد.
- آواتار صاحب برند در هدر از حالت overlay روی گوشه‌ی لوگو به یک آواتار مستقل کنار نام برند منتقل شد.
- متن‌های برند/گرید/حجم/تعداد کارتن در کارت محصول بزرگ‌تر و بولدتر شد.
- `npm run dev` کلاینت حالا با `server.open: true` خودش مرورگر را باز می‌کند.

**پاکسازی ساختار:**
- حذف فایل‌های بلااستفاده: `App.css` (بولیرپلیت Vite، هیچ‌جا import نمی‌شد)، `components/HomeSlider.jsx` (کامپوننت کامل و بدون استفاده)، `pages/admin/AdminProducts.jsx` (صفحه‌ی یتیم، جایی importش نمی‌کرد).
- ساده‌سازی مسیر ادمین: زنجیره‌ی سه‌لایه‌ی بی‌فایده‌ی `Admin.jsx → admin/AdminDashboard.jsx → AdminPanel` به `Admin.jsx → AdminPanel` مستقیم تبدیل شد؛ پوشه‌ی `pages/admin/` حذف شد.
- حذف ۱۶ فایل لوگوی رنگی بلااستفاده (`assets/logo/*-blue/green/red/white*` و نسخه‌های png/jpg اضافی) — فقط دو نسخه‌ی svg که واقعاً استفاده می‌شوند باقی ماندند.
- حذف ۴۸ فایل آپلودی یتیم در `server/uploads/products` (عکس‌های قدیمی که به هیچ محصولی در دیتابیس لینک نبودند؛ از ۳۶۵ به ۳۱۷ فایل).
- پوشه‌ی `templates/` که در ریشه‌ی پروژه بود و به هیچ روتی سرو نمی‌شد (لینک دانلود «فایل نمونه» در پنل ادمین اصلاً وجود نداشت) به `server/templates/` منتقل شد، از سرور استاتیک سرو می‌شود، و لینک دانلود در `BulkTools.jsx` اضافه شد.
- `dir` اولیه‌ی `index.html` از `ltr` به `rtl` اصلاح شد تا با زبان پیش‌فرض (فارسی) هماهنگ باشد و از فلش لحظه‌ای چپ‌به‌راست موقع لود جلوگیری شود.
- خطای لینت در `scripts/generateImages.js` (استفاده از `process` بدون تعریف global) با اضافه‌کردن override نود در `eslint.config.js` رفع شد.

**بررسی build/lint:** `npm run build` و `npm run lint` هر دو بدون خطا پاس می‌شوند.

## نکات باز (پیشنهاد برای بعد)

- تست خودکار (unit/integration) روی هیچ‌کدام از کلاینت یا سرور تعریف نشده.
- فایل‌های آپلودی یتیم دوباره جمع می‌شوند مگر یک مکانیزم پاکسازی خودکار (مثلاً موقع حذف محصول) اضافه شود.
- `server/scripts/hashPassword.js` یک اسکریپت دستی برای تولید `ADMIN_PASSWORD_HASH` است؛ عمداً نگه داشته شد.
